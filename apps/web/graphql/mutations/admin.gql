# apps/web/graphql/mutations/admin.gql

# Mutation to update a specific AI feature configuration
mutation UpdateAiFeatureConfig($input: UpdateAiFeatureConfigInput!) {
  updateAiFeatureConfig(input: $input) {
    featureKey
    providerName
    modelName
    updatedAt
  }
}